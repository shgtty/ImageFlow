# ImageFlow

**ImageFlow** は、指定したローカルフォルダや ZIP ファイル内の画像を自動的に収集し、無限スクロールのデジタルサイネージやスライドショーのように滑らかに表示させることができる軽量な Web アプリケーションです。

## ✨ 主な機能

- **2つの表示モード**:
    - **ギャラリーモード**: 無限スクロールで多くの画像を一覧表示します。デジタルフォトフレームや店舗サイネージに最適です。
    - **デュアルビューモード**: 2枚の画像を画面いっぱいに左右に並べて表示します。自動送り（スライドショー）機能付き。
- **インテリジェントなデュアルビュー**:
    - 画像の縦横比を維持しながら、画面いっぱいに大きく表示します。
    - **横長の画像（風景写真など）を検出すると、自動的に1枚のみを大きく表示**するスマートレイアウト機能を搭載。
    - 画像切り替え時の自然なアニメーションを搭載。
- **滑らかな無限オートスクロール**: ギャラリーモードでは数千枚の画像をスムーズに流し続け、一番下まで到達するとシームレスにループします。
- **ZIP ファイルの直接読み込み**: ディレクトリ内の ZIP を自動検出し、メモリ上でオンデマンドに展開して画像をストリーミング表示します。
- **柔軟なソート機能**:
    - **ランダム**: すべての画像をシャッフル（ギャラリーモードのデフォルト設定）。
    - **フォルダ単位ランダム**: フォルダ内の画像は元の順序を維持しつつ、フォルダ自体の表示順をランダム化。
    - **ファイル名昇順**: ファイル名基準でソート（デュアルビューモードのデフォルト設定）。
- **多機能なフィルターと加工**:
    - **高度なフィルター設定**: UIのモーダルからキーワードを動的に追加・削除可能。AND/OR モードの切り替えや、有効なキーワードを上位に表示する自動ソート機能を搭載。
    - **色モード**: グレイ、セピア、ネガティブ、高コントラストに加え、**高彩度**、**ぼかし**、**ドット絵（Pixel art）**、**ブラウン管（CRT）**などの多彩なリアルタイムフィルタ。
- **動的な設定ファイル切り替えと直接編集**:
    - 起動時にディレクトリを指定することで、UIのモーダルから複数のテキスト設定ファイルをシームレスに切り替え可能。
    - **内蔵エディタ**: 切り替えるだけでなく、設定ファイルをブラウザ上で直接編集・保存が可能。
- **没入型UIと状態の永続化**:
    - **スマートUIオートハイド**: 操作がないと、コントローラーとマウスカーソルが自動的にフェードアウトします。
    - **シームレスな位置復元**: モード切替時やウィンドウのリサイズ時、表示していた画像の位置を自動的に特定し、文脈を維持したまま表示を継続します。
    - **カウントダウンプログレス**: 次の画像への切り替えタイミングを視覚的に表示。
    - **カーソルツールチップ**: マウスカーソルの横に、現在の画像の「フォルダ名 > ファイル名」を動的に表示。
- **設定の自動保存**: スクロール速度、列数、表示モード、各種フィルタ設定などはブラウザを閉じても次回起動時に自動で復元されます。

## 🚀 起動方法

1. **事前準備**: お使いの PC に [Node.js](https://nodejs.org/) がインストールされている必要があります。
2. 本フォルダ内にある `start_server.bat` をダブルクリックして実行します。
3. 自動でサーバーが起動し、既定のブラウザで `http://localhost:8000` が開きます。

## 🎮 操作方法

### 共通の操作
- `Enter` キー / **画面ダブルクリック**: フルスクリーン（全画面）表示の切り替え。
- `M` キー: ギャラリーモードとデュアルビューモードの切り替え。
- `R` キー: ソート順（ランダム / フォルダ単位ランダム / 昇順）の切り替え。
- `Space` キー: 動作（スクロールまたは自動送り）の「一時停止 / 再開」。
- `S` キー: 画面下部シークバーの表示切り替え。
- `C` キー: 色モード（フィルタ）の切り替え。
- `I` キー: カーソル横のファイル名表示の切り替え。
- `F` キー: **フィルター設定モーダル**の表示。キーワードの追加やAND/OR切り替えが可能です。
- `0` キー: すべての設定（表示モード、ソート順、色モードなど）を初期値にリセット。
- `ESC` キー: **モーダルを閉じる**、通常表示に戻る、またはフルスクリーンを解除します。通常ウィンドウ時ならアプリケーションを終了します。
- **マウス操作**:
    - 画面上でマウスを動かすと操作パネルが表示されます。
    - **中クリック（ホイール押し込み）**: フルスクリーン表示の切り替え。

### ギャラリーモード
- `↑` / `↓` キー: スクロール速度の加速 / 減速。
- `←` / `→` キー: 画像の列数（カラム数）の変更（←で減少、→で増加、1～10列）。
- `PageUp` / `PageDown` キー: 前のフォルダ / 次のフォルダへ移動（昇順ソート時のみ）。

### デュアルビューモード
- `←` / `→` キー: 1枚ずつ前の画像 / 次の画像へ移動。
- `↑` / `↓` キー / **左右クリック** / **マウスホイール回転**: 前のページ / 次のページへ移動。
- `Home` / `End` キー: 最初の画像 / 最後の画像へ移動。
- `PageUp` / `PageDown` キー: 前のフォルダ / 次のフォルダへ移動（昇順ソート時のみ）。
- `O` キー: 表示方向（左から右 / 右から左）の切り替え。
- 操作パネルの `+` / `-` ボタン: 自動送りの間隔（秒数）を変更。

## 📝 設定ファイル (.txt)

`config` フォルダ内にある設定ファイルを直接編集するか、アプリケーションのUI上からカスタマイズできます。
※ 行頭に `#` を付けるとコメントとして無視されます。

### 1. `config/folders.txt`
読み込みたいフォルダのフルパスまたは ZIP ファイルのパスを1行ずつ記述します。
* 例：`C:\Users\User\Pictures`
* 例：`D:\Archives\TravelFiles.zip`

### 2. `config/include.txt`
表示したい画像に含まれるべき文字列を記述します。
* `MODE:AND` (デフォルト): 記述した「すべての単語」を含む画像のみ表示。
* `MODE:OR`: 記述した「いずれかの単語」を含む画像を表示。

### 3. `config/exclude.txt`
除外したいパスに含まれる文字列を記述します。`include.txt` より優先されます。

---

> **💻 高度な使い方**
> 独自のテキストファイルを引数として渡すことが可能です。
> `node server/server.js "my_folders.txt" "my_include.txt" "my_exclude.txt"`
> 
> また、ディレクトリパスを引数として渡すと「設定ファイル切り替えモード」になり、UI上からそのディレクトリ内のテキストファイル一覧を選択・**直接編集**できるようになります。
> `node server/server.js "C:\MyConfigs"`